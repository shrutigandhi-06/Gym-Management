<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context=".client_main">

    <androidx.coordinatorlayout.widget.CoordinatorLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/client_recycler_view"
            android:layout_width="match_parent"
            android:layout_height="match_parent"/>

        <ImageView
            android:id="@+id/img_add_clients"
            android:layout_width="150dp"
            android:layout_height="150dp"
            android:src="@drawable/add_clients"
            android:layout_gravity="center"
            android:layout_marginBottom="155dp"
            android:visibility="gone"/>

        <ImageView
            android:id="@+id/img_no_clients"
            android:layout_width="150dp"
            android:layout_height="150dp"
            android:src="@drawable/nothing_found"
            android:layout_gravity="center"
            android:layout_marginBottom="155dp"
            android:visibility="gone"/>

        <TextView
            android:id="@+id/txt_add_clients"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Add clients"
            android:layout_gravity="center"
            android:textSize="25dp"
            android:textColor="#fff"
            android:visibility="gone"/>

        <TextView
            android:id="@+id/no_clients"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="No client(s) found"
            android:layout_gravity="center"
            android:textSize="25dp"
            android:textColor="#fff"
            android:visibility="gone"/>

        <com.google.android.material.floatingactionbutton.FloatingActionButton
            android:id="@+id/btn_client_add"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:src="@drawable/ic_add"
            app:layout_anchor="@id/client_recycler_view"
            android:layout_marginRight="30dp"
            android:layout_marginBottom="30dp"
            app:layout_anchorGravity="bottom|right"/>

    </androidx.coordinatorlayout.widget.CoordinatorLayout>

    </LinearLayout>
